# Icon assets

These are 48 × 48 monochrome SVG source icons used by the prototype.

For SquareLine/LVGL, either:

1. import SVG if the target build includes an SVG decoder, or
2. export each icon as a monochrome A8/PNG image and recolor it per metric.

The black stroke is intentional so the assets can be recolored consistently.
